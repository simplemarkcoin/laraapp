FROM php:8.3-apache

# Install PHP extensions commonly used in Laravel or modern PHP apps
RUN apt-get update && apt-get install -y \
    libpng-dev \
    libonig-dev \
    libxml2-dev \
    zip \
    unzip \
    git \
    && docker-php-ext-install pdo_mysql mbstring bcmath gd
